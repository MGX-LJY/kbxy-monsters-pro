# 通用说明：把此文件复制成 .env.dev / .env.test 并按需填写

# dev 或 test（本项目只用这两个）
APP_ENV=dev

# 使用本地 SQLite（默认） → 最终落在 <项目根>/data/<自动文件名>
#KBXY_DB_PATH=kbxy-dev.db
#KBXY_DB_PATH=/absolute/path/to/file.db

# 使用外部数据库（设置后会忽略本地 SQLite 路径）
#DATABASE_URL=sqlite+pysqlite:////absolute/path/to/file.db
#DATABASE_URL=postgresql+psycopg2://user:pass@host:5432/dbname
#DATABASE_URL=mysql+pymysql://user:pass@host:3306/dbname